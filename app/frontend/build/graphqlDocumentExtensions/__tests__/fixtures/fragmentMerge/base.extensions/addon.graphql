# Copyright (C) 2012-2026 Zammad Foundation, https://zammad-foundation.org/
# `User` is a member of the `Item` union returned by `items` — type-driven
# placement must attach the spread there.

fragment userDetailSearch on User {
  id
  fullname
}
