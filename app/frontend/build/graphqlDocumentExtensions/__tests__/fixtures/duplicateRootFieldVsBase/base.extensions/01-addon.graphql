# Copyright (C) 2012-2026 Zammad Foundation, https://zammad-foundation.org/
# Aliases its root field as `search`, colliding with the BASE document's own
# (unaliased) `search` root field — not another extension.

query addon($search: String!) {
  search: search(search: $search) {
    totalCount
  }
}
