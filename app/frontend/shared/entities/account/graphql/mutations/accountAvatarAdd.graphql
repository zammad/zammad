mutation accountAvatarAdd($images: AvatarInput!) {
  accountAvatarAdd(images: $images) {
    avatar {
      id
      default
      deletable
      initial
      imageFull
      imageResize
      imageHash
      createdAt
      updatedAt
    }
    errors {
      ...errors
    }
  }
}
