query user($userId: ID!, $secondaryOrganizationsCount: Int) {
  user(userId: $userId) {
    ...userDetailAttributes
    hasSecondaryOrganizations
    ticketsCount {
      open
      openSearchQuery
      closed
      closedSearchQuery
    }
    policy {
      update
    }
  }
}
