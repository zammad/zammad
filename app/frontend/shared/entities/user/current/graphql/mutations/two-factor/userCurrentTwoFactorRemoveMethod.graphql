mutation userCurrentTwoFactorRemoveMethod(
  $methodName: String!
  $token: String!
) {
  userCurrentTwoFactorRemoveMethod(
    methodName: $methodName
    token: $token
  ) {
    success
    errors {
      ...errors
    }
  }
}
