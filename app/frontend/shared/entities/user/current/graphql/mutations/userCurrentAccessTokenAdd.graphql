mutation userCurrentAccessTokenAdd($input: UserAccessTokenInput!) {
  userCurrentAccessTokenAdd(input: $input) {
    token {
      ...tokenAttributes
    }
    tokenValue
    errors {
      ...errors
    }
  }
}
