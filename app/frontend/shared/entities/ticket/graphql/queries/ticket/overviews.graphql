query ticketOverviews($withTicketCount: Boolean!) {
  ticketOverviews {
    edges {
      node {
        ...overviewAttributes
      }
      cursor
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}
