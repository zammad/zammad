fragment overviewAttributes on Overview {
  id
  internalId
  name
  link
  prio
  groupBy
  orderBy
  orderDirection
  organizationShared
  outOfOffice
  active
  ticketCount @include(if: $withTicketCount)
}
