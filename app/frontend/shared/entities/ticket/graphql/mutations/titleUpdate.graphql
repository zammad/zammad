mutation ticketTitleUpdate($ticketId: ID!, $title: String!) {
  ticketTitleUpdate(ticketId: $ticketId, title: $title) {
    ticket {
      ...ticketAttributes
    }
    errors {
      ...errors
    }
  }
}
