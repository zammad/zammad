subscription ticketLiveUserUpdates(
  $userId: ID!
  $key: String!
  $app: EnumTaskbarApp!
) {
  ticketLiveUserUpdates(userId: $userId, key: $key, app: $app) {
    liveUsers {
      ...ticketLiveUserAttributes
    }
  }
}
