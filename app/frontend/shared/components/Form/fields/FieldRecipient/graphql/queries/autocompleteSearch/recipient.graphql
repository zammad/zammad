# TODO: We'll probably need to move/copy this directly into the recipient component.
query autocompleteSearchRecipient($input: AutocompleteSearchInput!) {
  autocompleteSearchRecipient(input: $input) {
    value
    label
    labelPlaceholder
    heading
    headingPlaceholder
    disabled
    icon
  }
}
