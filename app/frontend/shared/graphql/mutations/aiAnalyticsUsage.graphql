mutation aiAnalyticsUsage(
  $aiAnalyticsRunId: ID!
  $input: AIAnalyticsUsageInput!
) {
  aiAnalyticsUsage(
    aiAnalyticsRunId: $aiAnalyticsRunId
    input: $input
  ) {
    usage {
      id
    }
  }
}
