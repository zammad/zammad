subscription ticketUpdates($ticketId: ID!) {
  ticketUpdates(ticketId: $ticketId) {
    ticket {
      ...ticketAttributes
      articleCount
      policy {
        update
      }
      mentions {
        totalCount
        edges {
          node {
            ...ticketMention
          }
          cursor
        }
      }
    }
  }
}
