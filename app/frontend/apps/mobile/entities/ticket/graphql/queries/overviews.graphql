query ticketOverviews($withTicketCount: Boolean!) {
  ticketOverviews(ignoreUserConditions: false) {
    ...overviewAttributes
    viewColumns {
      key
      value
    }
    orderColumns {
      key
      value
    }
  }
}
