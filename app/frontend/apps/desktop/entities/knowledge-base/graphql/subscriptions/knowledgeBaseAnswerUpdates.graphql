subscription knowledgeBaseAnswerUpdates(
  $answerId: ID!
  $locale: String
  $initial: Boolean = false
) {
  knowledgeBaseAnswerUpdates(answerId: $answerId, locale: $locale, initial: $initial) {
    answer {
      id
      ...knowledgeBaseAnswerAttributes
    }
  }
}
