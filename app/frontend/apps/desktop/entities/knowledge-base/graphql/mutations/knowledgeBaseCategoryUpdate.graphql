mutation knowledgeBaseCategoryUpdate(
  $categoryId: ID!
  $input: KnowledgeBaseCategoryInput!
  $locale: String!
) {
  knowledgeBaseCategoryUpdate(categoryId: $categoryId, input: $input, locale: $locale) {
    category {
      ...knowledgeBaseCategoryGridAttributes
    }
    errors {
      ...errors
    }
  }
}
