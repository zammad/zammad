subscription userCurrentTaskbarItemListUpdates(
  $userId: ID!
  $app: EnumTaskbarApp!
) {
  userCurrentTaskbarItemListUpdates(userId: $userId, app: $app) {
    taskbarItemList {
      id
      prio
    }
  }
}
