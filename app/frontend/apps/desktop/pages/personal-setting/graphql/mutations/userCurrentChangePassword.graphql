mutation userCurrentChangePassword(
  $currentPassword: String!
  $newPassword: String!
) {
  userCurrentChangePassword(
    currentPassword: $currentPassword
    newPassword: $newPassword
  ) {
    success
    errors {
      ...errors
    }
  }
}
