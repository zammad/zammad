Reimposta la password su #{config.product_name}

<div>Buongiorno #{user.firstname},</div>
<br>
<div>tu o qualcuno in tua vece ha richiesto l'iscrizione con questo indirizzo email. Purtroppo questo indirizzo è stato già utilizzato.</div>
<br>
<div>Se vuoi reimpostare la tua password, clicca sul collegamento sotto (o fai copia/incolla nel browser):</div>
<br>
<div><a href="#{config.http_type}://#{config.fqdn}/#password_reset_verify/#{token.name}">#{config.http_type}://#{config.fqdn}/#password_reset_verify/#{token.name}</a></div>
<br>
<div>Questo collegamento porta a una pagina dove puoi modificare la tua password.</div>
<br>
<div>Se non vuoi cambiare la password, ignora questo messaggio. La tua password non verrà modificata.</div>
<br>
<div>Il team #{config.product_name}</div>
