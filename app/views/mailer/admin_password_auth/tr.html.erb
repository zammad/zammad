#{config.product_name} için yönetim girişi

<div>Merhaba #{user.firstname},</div>
<br>
<div>#{config.product_name} hesabınız için bir yönetim girişi talebi aldık <b>#{user.login}</b>.</div>
<br>
<div>Devam etmek için lütfen aşağıdaki bağlantıya tıklayın (veya URL'yi kopyalayıp tarayıcınıza yapıştırın):</div>
<br>
<div><a href="#{config.http_type}://#{config.fqdn}/#login/admin/#{token.token}">#{config.http_type}://#{config.fqdn}/#login/admin/#{token.token}</a></div>
<br>
<div>Bu bağlantı sizi giriş yapabileceğiniz bir sayfaya yönlendirir.</div>
<br>
<div>Giriş yapmak istemiyorsanız, lütfen bu mesajı dikkate almayın.</div>
<br>
<div>#{config.product_name} Ekibiniz</div>
