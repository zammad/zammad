邀請您加入 #{config.product_name}（平台位址：#{config.fqdn}）

<div>您好，#{user.firstname}：</div>
<br>
<div>我是 #{current_user.firstname} #{current_user.lastname}，誠摯邀請您加入 #{config.product_name} —— 我們的客戶支援平台。</div>
<br>
<div>請點擊 <a href="#{config.http_type}://#{config.fqdn}/#password_reset_verify/#{token.token}">這裡</a> 設定您的密碼。</div>
<br>
<div>祝 使用愉快，</div>
<br>
<div>#{current_user.firstname} #{current_user.lastname}</div>
<br>
<div>#{config.product_name} 團隊 敬上</div>
