Meghívó a(z) #{config.fqdn} helyen lévő #{config.product_name} rendszerbe

<div>Kedves #{user.firstname}!</div>
<br>
<div>Én (#{current_user.lastname} #{current_user.firstname}) meghívom Önt a(z) #{config.product_name} rendszerbe – az ügyfélszolgálati platformunkra.</div>
<br>
<div>Kattintson <a href="#{config.http_type}://#{config.fqdn}/#password_reset_verify/#{token.token}">ide</a>, és állítsa be a jelszavát.</div>
<br>
<div>Sok sikert!</div>
<br>
<div>#{current_user.lastname} #{current_user.firstname}</div>
<br>
<div>Az Ön #{config.product_name} csapata</div>
