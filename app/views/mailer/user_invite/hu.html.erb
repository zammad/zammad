Meghívó a #{config.product_name} helyére a #{config.fqdn} webhelyre.

<div>Üdvözöljük #{user.firstname},</div>
<br>
<div>Én (#{current_user.firstname} #{current_user.lastname}) meghívom Önt a #{config.product_name} - ügyfélszolgálati platformunkra.</div>
<br>
<div>Kattintson a <a href="#{config.http_type}://#{config.fqdn}/#password_reset_verify/#{token.name}">ide</a> és állítsa be a jelszavát.</div>
<br>
<div>Sok sikert,</div>
<br>
<div>#{current_user.firstname} #{current_user.lastname}</div>
<br>
<div>Az Ön #{config.product_name} csapata</div>
