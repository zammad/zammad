Pozvánka do #{config.product_name} na #{config.fqdn}

<div>Dobrý deň, #{user.firstname},</div>
<br>
<div>Ja (#{current_user.firstname} #{current_user.lastname}) vás pozývam do #{config.product_name} - našej platformy zákazníckej podpory.</div>
<br>
<div>Kliknite <a href="#{config.http_type}://#{config.fqdn}/#password_reset_verify/#{token.token}">sem</a> a nastavte si svoje heslo.</div>
<br>
<div>S pozdravom,</div>
<br>
<div>#{current_user.firstname} #{current_user.lastname}</div>
<br>
<div>Váš tím #{config.product_name}</div>
