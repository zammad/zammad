Inbjudan till #{config.product_name}-konto på #{config.fqdn}

<div>Hej #{user.firstname},</div>
<br>
<div>(#{current_user.firstname} #{current_user.lastname}) bjuder in dig till #{config.product_name} - vår supportplattform.</div>
<br>
<div>Klicka <a href="#{config.http_type}://#{config.fqdn}/#password_reset_verify/#{token.token}">här</a> och bestäm ditt lösenord.</div>
<br>
<div>Njut,</div>
<br>
<div>#{current_user.firstname} #{current_user.lastname}</div>
<br>
<div>Från ditt #{config.product_name} Team</div>
