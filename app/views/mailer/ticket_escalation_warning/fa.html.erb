تیکت تشدید خواهد شد (#{ticket.title})

<div>سلام #{recipient.firstname}،</div>
<br>
<div>تیکت (#{ticket.title}) از "<b>#{ticket.customer.longname}</b>" در "#{ticket.escalation_at}" تشدید خواهد شد!</div>
<br>
<% if @objects[:article] %>
  <div>
  اطلاعات:
  <blockquote type="cite">
  #{article.body_as_html}
  </blockquote>
  </div>
<% end %>
<br>
<div>
  <a href="#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}" target="zammad_app">مشاهده این در #{config.product_name}</a>
</div>
