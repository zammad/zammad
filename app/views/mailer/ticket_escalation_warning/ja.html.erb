チケットがエスカレーションされます（#{ticket.title}

<div>こんにちは、#{recipient.firstname}さん。</div>
<br>
<div>「<b>#{ticket.customer.longname}</b>」からのチケット（#{ticket.title}）が「#{ticket.escalation_at}」にエスカレーションされます！</div>
<br>
<% if @objects[:article] %>
  <div>
  情報:
  <blockquote type="cite">
  #{article.body_as_html}
  </blockquote>
  </div>
<% end %>
<br>
<div>
  <a href="#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}" target="zammad_app">これを #{config.product_name} で表示する</a>
</div>
