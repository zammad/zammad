Tiket bude eskalovať (#{ticket.title})

<div>Dobrý deň, #{recipient.firstname},</div>
<br>
<div>Tiket (#{ticket.title}) od "<b>#{ticket.customer.longname}</b>" bude eskalovať "#{ticket.escalation_at}"!</div>
<br>
<% if @objects[:article] %>
  <div>
  Informácia:
  <blockquote type="cite">
  #{article.body_as_html}
  </blockquote>
  </div>
<% end %>
<br>
<div>
  <a href="#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}" target="zammad_app">Zobraziť v #{config.product_name}</a>
</div>
