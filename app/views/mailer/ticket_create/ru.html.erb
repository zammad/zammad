Новая заявка (#{ticket.title})

<div>Здравствуйте, #{recipient.firstname}.</div>
<br>
<div>Новая заявка (#{ticket.title}) была создана "<b>#{current_user.longname}</b>".</div>
<br>
<div>
Группа: #{ticket.group.name}<br>
Владелец: #{ticket.owner.fullname}<br>
Состояние: #{t(ticket.state.name)}<br>
</div>
<br>
<% if @objects[:article] %>
  <div>
  Информация:
  <blockquote type="cite">
  #{article.body_as_html}
  </blockquote>
  </div>
<% end %>
<br>
<div>
  <a href="#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}" target="zammad_app">Посмотреть это в #{config.product_name}</a>
</div>
