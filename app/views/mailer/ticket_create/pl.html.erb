<div class="zammad-email-container">
  <div class="zammad-email-notification">
    <div class="zammad-email-notification-header">
      <div class="row">
        <div class="col-md-2 col-sm-2 col-xs-3">
          <img src="<%= image_tag 'assets/images/icons.svg' %>" class="zammad-email-notification-image img-circle">
        </div>
        <div class="col-md-10 col-sm-10 col-xs-9">
          <h1 class="zen-title">Nowe zgłoszenie: (#{ticket.title})</h1>
          <p class="zen-content">Odbiorca: #{recipient.firstname}</p>
        </div>
      </div>
    </div>
    <div class="zammad-email-notification-body">
      Witaj #{recipient.firstname},<br><br>

      Otrzymałeś nowe zgłoszenie (#{ticket.title}) od "<strong>#{current_user.longname}</strong>". <br><br>

      #{t('Group')}: #{ticket.group.name} <br>
      #{t('Owner')}: #{ticket.owner.fullname} <br>
      #{t('State')}: #{t(ticket.state.name)} <br><br>

      <% if @objects[:article] %>
        #{t('Information')}:<br><br>

        <blockquote>
          #{article.body_as_html}
        </blockquote>
      <% end %>
    </div>
    <div class="zammad-email-notification-footer">
      <div class="text-center">
        <a href="#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}" target="zammad_app">#{t('View this in Zammad')}</a>
      </div>
    </div>
  </div>
</div>

<!-- Nowe zgłoszenie (#{ticket.title})

<div>Witaj #{recipient.firstname},</div>
<br>
<div>Otrzymałeś nowe zgłoszenie (#{ticket.title}) od "<b>#{current_user.longname}</b>".</div>
<br>
<div>
#{t('Group')}: #{ticket.group.name}<br>
#{t('Owner')}: #{ticket.owner.fullname}<br>
#{t('State')}: #{t(ticket.state.name)}<br>
</div>
<br>
<% if @objects[:article] %>
  <div>
  #{t('Information')}:
  <blockquote type="cite">
  #{article.body_as_html}
  </blockquote>
  </div>
<% end %>
<br>
<div>
  <a href="#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}" target="zammad_app">#{t('Zobacz w Zammad')}</a>
</div> -->
