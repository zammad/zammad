تیکت (#{ticket.title}) در تیکت دیگری ادغام شد

<div>سلام #{recipient.firstname}،</div>
<br>
<div>
تیکت (#{ticket.title}) توسط "<b>#{current_user.longname}</b>" در تیکت دیگری ادغام شد.
</div>
<br>
<div>
  <a href="#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}" target="zammad_app">مشاهده این در #{config.product_name}</a>
</div>
