یادآوری تیکت رسید (#{ticket.title})

<div>سلام #{recipient.firstname}،</div>
<br>
<div>یک تیکت نیاز به توجه دارد: یادآوری برای (#{ticket.title}) با مشتری "<b>#{ticket.customer.longname}</b>" رسید.</div>
<br>
<% if @objects[:article] %>
  <div>
  اطلاعات:
  <blockquote type="cite">
  #{article.body_as_html}
  </blockquote>
  </div>
<% end %>
<br>
<div>
  <a href="#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}" target="zammad_app">مشاهده این در #{config.product_name}</a>
</div>
