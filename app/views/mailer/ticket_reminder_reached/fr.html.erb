Rappel du ticket atteint (#{ticket.title})

<div>Bonjour #{recipient.firstname},</div>
<br>
<div>Un ticket nécessite de l'attention, un rappel a été atteint pour (#{ticket.title}) du client "<b>#{ticket.customer.longname}</b>".</div>
<br>
<% if @objects[:article] %>
  <div>
  Informations :
  <blockquote type="cite">
  #{article.body_as_html}
  </blockquote>
  </div>
<% end %>
<br>
<div>
  <a href="#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}" target="zammad_app">Voir dans #{config.product_name}</a>
</div>
