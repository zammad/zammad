مقاله تیکت به‌روزرسانی شد (#{ticket.title})

<div>سلام #{recipient.firstname}،</div>
<br>
<div>
<%= @objects[:article].preferences.dig('whatsapp', 'reaction', 'author') %> با <%= @objects[:article].preferences.dig('whatsapp', 'reaction', 'emoji') %> به پیام از #{article.created_by.fullname} واکنش نشان داد:
</div>
<br>
<% if @objects[:article] %>
  <div>
    <blockquote type="cite">
    #{article.body_as_html}
    </blockquote>
  </div>
<% end %>
<br>
<div>
  <a href="#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}/#{article.id}" target="zammad_app">این را در #{config.product_name} مشاهده کنید</a>
</div>
