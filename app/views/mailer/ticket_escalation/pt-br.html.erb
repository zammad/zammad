Ticket escalado (#{ticket.title})

<div>Olá, #{recipient.firstname},</div>
<br>
<div>O ticket (#{ticket.title}) de "<b>#{ticket.customer.longname}</b>" foi escalado desde "#{ticket.escalation_at}"!</div>
<br>
<% if @objects[:artigo] %>
  <div>
  Detalhes:
  <blockquote type="cite">
  #{article.body_as_html}
  </blockquote>
  </div>
<% end %>
<br>
<div>
  <a href="#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}" target="zammad_app">Veja isso em #{config.product_name}</ a>
</div>
