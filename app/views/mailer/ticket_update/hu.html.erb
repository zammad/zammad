Frissített jegy (#{ticket.title})

<div>Kedves #{recipient.firstname}!</div>
<br>
<div>
A jegyet (#{ticket.title}) „<b>#{current_user.longname}</b>” frissítette.
</div>
<br>
<% if @objects[:changes].present? %>
  <div>
  Változtatások:<br>
  <% @objects[:changes].each do |key, value| %>
    <%= t key %>: <%= h value[0] %> -&gt; <%= h value[1] %><br>
  <% end %>
  </div>
<% end %>
<br>
<% if @objects[:article] %>
  <div>
  Információ:
  <blockquote type="cite">
  #{article.body_as_html}
  </blockquote>
  </div>
<% end %>
<br>
<div>
  <a href="#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}<% if @objects[:article] %>/#{article.id}<% end %>" target="zammad_app">Nézze meg itt: #{config.product_name}</a>
</div>
