[قابل تحویل نیست] پیام خیلی بزرگ است
عزیز #{mail.from_display_name}،

متاسفانه ایمیل شما با عنوان "#{mail.subject}" نمی‌تواند به یک یا چند گیرنده تحویل داده شود.

پیام شما #{mail.msg_size} MB بود اما ما فقط پیام‌های تا #{config.postmaster_max_size} MB را می‌پذیریم.

لطفاً اندازه پیام را کاهش دهید و دوباره تلاش کنید. از درک شما متشکریم.

با تأسف،

پست مستر #{config.fqdn}
