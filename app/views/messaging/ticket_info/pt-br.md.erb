# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|#{config.ticket_hook}#{ticket.number}>: Última atualização em #{ticket.updated_at}_
* Grupo: #{ticket.group.name}
* Responsável: #{ticket.owner.fullname}
* Estado: #{t(ticket.state.name)}

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
