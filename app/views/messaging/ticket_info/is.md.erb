# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|Ticket##{ticket.number}>: Síðast uppfærð þann #{ticket.updated_at}_
* Hópur: #{ticket.group.name}
* Eigandi: #{ticket.owner.fullname}
* Staða: #{t(ticket.state.name)}

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
