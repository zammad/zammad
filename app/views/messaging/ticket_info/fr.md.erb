# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|#{config.ticket_hook}#{ticket.number}> : Dernière mise à jour le #{ticket.updated_at}_
* Groupe : #{ticket.group.name}
* Propriétaire : #{ticket.owner.fullname}
* Statut : #{t(ticket.state.name)}

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
