# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|#{config.ticket_hook}#{ticket.number}>: #{ticket.updated_at}-এ সর্বশেষ আপডেট হয়েছে_
* গ্রুপ: #{ticket.group.name}
* মালিক: #{ticket.owner.fullname}
* অবস্থা: #{t(ticket.state.name)}

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
