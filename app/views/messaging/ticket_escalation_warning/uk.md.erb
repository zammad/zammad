# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|#{config.ticket_hook}#{ticket.number}>: Буде протерміновано о #{ticket.escalation_at}_
Заявка (#{ticket.title}) від"#{ticket.customer.longname}" буде протерміновано о "#{ticket.escalation_at}"!

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
