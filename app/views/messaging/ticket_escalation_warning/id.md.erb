# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|Tiket##{ticket.number}>: Akan dieskalasi pada #{ticket.escalation_at}_
Tiket (#{ticket.title}) dari "#{ticket.customer.longname}" akan dieskalasi pada "#{ticket.escalation_at}"!

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
