# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|#{config.ticket_hook}#{ticket.number}>: #{ticket.escalation_at} にエスカレーション予定です_
"#{ticket.customer.longname}" からのチケット (#{ticket.title}) は "#{ticket.escalation_at}" にエスカレーションされます！

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
