# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|Tiket##{ticket.number}>: Bude eskalován #{ticket.escalation_at}_
Tiket (#{ticket.title}) od "#{ticket.customer.longname}" bude eskalován "#{ticket.escalation_at}"!

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
