# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|#{config.ticket_hook}#{ticket.number}>: Escalonará em #{ticket.escalation_at}_
O ticket (#{ticket.title}) de "#{ticket.customer.longname}" escalonará em "#{ticket.escalation_at}"!

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
