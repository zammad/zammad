# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|#{config.ticket_hook}#{ticket.number}>: #{ticket.escalation_at}-এ এস্কেলেট হবে_
"#{ticket.customer.longname}" থেকে আসা টিকিট (#{ticket.title}) "#{ticket.escalation_at}"-এ এস্কেলেট হবে!

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
