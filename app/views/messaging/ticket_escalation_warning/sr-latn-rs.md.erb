# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|Ticket##{ticket.number}>: Eskaliraće u #{ticket.escalation_at}_
Tiket (#{ticket.title}) od „#{ticket.customer.longname}” eskaliraće u „#{ticket.escalation_at}”!

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
