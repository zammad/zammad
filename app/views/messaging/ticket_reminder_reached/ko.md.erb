# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#티켓/줌/#{티켓.id}|티켓##{티켓.번호}>: 알림이 도착했습니다!_
티켓에 주의가 필요합니다: 고객 "*#{ticket.customer.longname}*"의 (#{ticket.title})에 대한 알림이 도달했습니다.

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
