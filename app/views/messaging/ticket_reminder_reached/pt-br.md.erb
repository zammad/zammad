# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|Tíquete##{ticket.number}>: Lembrete!_
Um tíquete precisa de atenção, lembrete criado para (#{ticket.title}) com usuário "*#{ticket.customer.longname}*".

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
