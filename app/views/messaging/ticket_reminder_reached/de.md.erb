# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|#{config.ticket_hook}#{ticket.number}>: Erinnerung erreicht!_
Dieses Ticket benötigt Ihre Aufmerksamkeit: Warten auf Erinnerung erreicht für (#{ticket.title}) von Kunde "*#{ticket.customer.longname}*".

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
