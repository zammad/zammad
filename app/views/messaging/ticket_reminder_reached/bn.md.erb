# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|#{config.ticket_hook}#{ticket.number}>: রিমাইন্ডারের সময় হয়েছে!_
একটি টিকিটে মনোযোগ প্রয়োজন: "*#{ticket.customer.longname}*" গ্রাহকের (#{ticket.title}) টিকিটের রিমাইন্ডারের সময় হয়েছে।

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
