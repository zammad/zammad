# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|#{config.ticket_hook}#{ticket.number}>: Emlékeztető elérve!_
Egy jegy figyelmet igényel: emlékeztető elérve „*#{ticket.customer.longname}*” ügyfél (#{ticket.title}) jegynél.

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
