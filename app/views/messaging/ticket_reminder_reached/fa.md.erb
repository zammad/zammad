# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|Ticket##{ticket.number}>: یادآوری رسید!_
یک بلیط نیاز به توجه دارد: یادآوری به آن رسید (#{ticket.title}) با مشتری "*#{ticket.customer.longname}*".

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
