# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|Tiket##{ticket.number}>: Pengingat tercapai!_
Sebuah tiket memerlukan perhatian: pengingat tercapai untuk (#{ticket.title}) dengan pelanggan "*#{ticket.customer.longname}*".

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
