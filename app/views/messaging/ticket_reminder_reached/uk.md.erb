# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|Ticket##{ticket.number}>: Досягнуто нагадування!_
Заявка потребує уваги: досягнуто нагадування для (#{ticket.title}) від клієнта "*#{ticket.customer.longname}*".

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
