# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|#{config.ticket_hook}#{ticket.number}>: Oprettet af #{current_user.longname} kl #{ticket.updated_at}_
* Gruppe: #{ticket.group.name}
* Ejer: #{ticket.owner.fullname}
* Status: #{t(ticket.state.name)}

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
