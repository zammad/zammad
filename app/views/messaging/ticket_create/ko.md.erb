# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#티켓/줌/#{티켓.id}|티켓##{티켓.번호}>: 현재_사용자.긴이름}이 #{티켓.업데이트된_날짜}_에서 생성했습니다_
* 그룹: #{ticket.group.name}
* 소유자: #{ticket.owner.fullname}
* 상태: #{t(ticket.state.name)}

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
