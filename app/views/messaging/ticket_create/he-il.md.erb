# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|כרטיס##{ticket.number}>: נוצר על ידי #{current_user.longname} ב-# {ticket.updated_at}_
* קבוצה: #{ticket.group.name}
* בעלים: #{ticket.owner.fullname}
* סטטוס: #{t(ticket.state.name)}

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
