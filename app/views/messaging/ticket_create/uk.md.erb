# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|Ticket##{ticket.number}>: Створено #{current_user.longname} at #{ticket.updated_at}_
* Група: #{ticket.group.name}
* Власник: #{ticket.owner.fullname}
* Статус: #{t(ticket.state.name)}

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
