# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|#{config.ticket_hook}#{ticket.number}>: #{current_user.longname} hozta létre ekkor: #{ticket.updated_at}_
* Csoport: #{ticket.group.name}
* Tulajdonos: #{ticket.owner.fullname}
* Állapot: #{t(ticket.state.name)}

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
