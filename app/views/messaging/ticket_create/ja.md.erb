# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|#{config.ticket_hook}#{ticket.number}>: #{current_user.longname} が #{ticket.updated_at} に作成しました_
* グループ: #{ticket.group.name}
* 所有者: #{ticket.owner.fullname}
* 状態: #{t(ticket.state.name)}

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
