# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|تذكرة##{ticket.number}>: تم تحديثها بواسطة #{current_user.longname} في #{ticket.updated_at}_
<% if @objects[:changes].present? %>
  <% @objects[:changes].each do |key, value| %>
  * <%= t key %>: <%= h value[0] %> -> <%= h value[1] %>
  <% end %>
<% end %>

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
