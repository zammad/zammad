# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|Tiket##{ticket.number}>: Eskalován #{ticket.escalation_at}_
Tiket (#{ticket.title}) od "#{ticket.customer.longname}" je v eskalaci od "#{ticket.escalation_at}"!

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
