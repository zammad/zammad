# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|Ticket##{ticket.number}>: Escalado el #{ticket.escalation_at}_
El tiquete (#{ticket.title}) de "#{ticket.customer.longname}" ha sido escalado desde "#{ticket.escalation_at}"!

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
