# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#티켓/줌/#{티켓.id}|티켓##{티켓.번호}>: 티켓 에스컬레이션 주소 #{ticket.escalation_at}_에서 에스컬레이션됨_
"#{ticket.customer.longname}"의 티켓(#{ticket.title})이 "#{ticket.escalation_at}" 이후 에스컬레이션되었습니다!

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
