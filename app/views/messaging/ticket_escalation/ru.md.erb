# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|Ticket##{ticket.number}>: Эскалирована с #{ticket.escalation_at}_
Заявка (#{ticket.title}) от "#{ticket.customer.longname}" была эскалирована с "#{ticket.escalation_at}"!

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
