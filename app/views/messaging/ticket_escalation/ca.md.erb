# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|Ticket##{ticket.number}>: Escalat el #{ticket.escalation_at}_
El tiquet (#{ticket.title}) de "#{ticket.customer.longname}" s'ha escalat des de "#{ticket.escalation_at}"!

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
