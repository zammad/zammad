# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|#{config.ticket_hook}#{ticket.number}>: eszkalálva ekkor: #{ticket.escalation_at}_
„#{ticket.customer.longname}” jegye (#{ticket.title}) eszkalálva lett ekkortól: „#{ticket.escalation_at}”!

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
