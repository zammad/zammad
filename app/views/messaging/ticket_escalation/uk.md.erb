# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|#{config.ticket_hook}#{ticket.number}>: Протермінування о #{ticket.escalation_at}_
Заявку (#{ticket.title}) від "#{ticket.customer.longname}" було протерміновано з "#{ticket.escalation_at}"!

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
