# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|#{config.ticket_hook}#{ticket.number}>: #{ticket.escalation_at}-এ এস্কেলেটেড হয়েছে_
"#{ticket.customer.longname}" থেকে আসা টিকিট (#{ticket.title}) "#{ticket.escalation_at}" থেকে এস্কেলেটেড হয়েছে!

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
