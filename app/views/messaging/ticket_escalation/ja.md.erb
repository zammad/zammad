# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|#{config.ticket_hook}#{ticket.number}>: #{ticket.escalation_at} にエスカレーションされました_
"#{ticket.customer.longname}" からのチケット (#{ticket.title}) は "#{ticket.escalation_at}" からエスカレーションされています！

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
