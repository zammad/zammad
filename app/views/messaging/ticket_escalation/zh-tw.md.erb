# #{ticket.title}
_<#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}|Ticket##{ticket.number}>: 從 #{ticket.escalation_at} 升級_
來自 "#{ticket.customer.longname}" 的工單 (#{ticket.title}) 已從 "#{ticket.escalation_at}" 升級

<% if @objects[:article] %>
#{article.body_as_text}
<% end %>
