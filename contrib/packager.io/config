#!/bin/bash
#
# packager.io postinstall script config
#

ZAMMAD_DIR="/opt/zammad"
DB="zammad"
DB_USER="zammad"
ZAMMAD_WEBS="1"
ZAMMAD_WEBSOCKETS="1"
ZAMMAD_WORKERS="1"
DEBUG="no"
WEBSERVER_CONF="${WEBSERVER_CONF:-}"
WEBSERVER_SITES_ENABLED="${WEBSERVER_SITES_ENABLED:-}"
WEBSERVER_CONFIG_SKIP="${WEBSERVER_CONFIG_SKIP:-no}"
