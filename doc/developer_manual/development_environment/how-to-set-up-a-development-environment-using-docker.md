# How to Set Up A Development Environment for Zammad using docker & docker compose

## Run `bin/docker-setup` shortcut at first start

It will:

- Create blank .env file
- Build docker image
- Install javascript and ruby dependencies into volumes

## Then run `docker compose up` to start application
